git init 
- initializes an empty repository

git status
-gives information of the current  state of  git repository

git checkout <file name>
-switching to nre branch  if you want to switch to an exisiting branch then use git checkout  branch with -b

git add <file name>
- use to stage the untracked file

git branch 
- gives the info

git commit -m "<youer message>"
- used to track the staged file after being added by git add

git push origin main

git pull orgin main 